var config = {
    _default : {
        PORT: '3344',
        DOMAIN : 'example.com',
        basicAuth : {
            user: 'username',
            pass: 'password',
        }
    },
}

module.exports.config = config;
